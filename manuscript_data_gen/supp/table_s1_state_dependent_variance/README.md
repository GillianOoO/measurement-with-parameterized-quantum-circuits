# Supplementary table: state-dependent variance, bias, and MSE

Input: the audited `supp_state_dependent_variance_T3000.csv`. The builder emits
the tabular body containing the exact fixed-design variance, normalized
coefficient, schedule-induced i.i.d. variance, signed approximation bias, and
`MSE = variance + bias^2`.
